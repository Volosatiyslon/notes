# voice-bots-adapter revert connection

1 добавить в адаптер brief-rpc ручку с контрактом, похожим на brief/rpc/service/dial.brief/DialRequest, но расширенным полем callID
2 добавить в Manager (cmd/adapter/manager.go) дополнительный метод, сохраняющий rpcprotocol.StreamSendReceiveCloser - это будет "канал" с сервисом voice-bots
3 добавить хендлер ручки, созданный в п1 который сохраняет  полученный канал в manager
5 переделать обработчик звонка (cmd/adapter/manager.go) на использование сохраненного канала (может имеет смысл переделать копию): вместо s.Send s.Recv использовать mgr.s.Send и mgr.s.Recv
6 
7









------
1 добавить в voice-bots "адаптер" к адаптеру
2 переделать (добавить) usecase internal/bots/usecases/calls/manager.go таким образом, что бы он принимал инвайт из существующего канала полученого из адаптера адаптера

------
0 собрать сервис эхо, который принимает по стриму текст и ответчает "вы сказали <изначальный текст>", прикрутить его к voice-bots вместо демо модельки. (для полноты тестов стоит добавить когда эхо кладет трубку)
1 раскатить адаптер на медианоду рядом с пилотным
2 (Рома?) настроить некоторое количество звонков в сторону экспериментального адаптера
------

upstream